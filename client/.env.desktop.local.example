# Optional local override (not committed if you rename to .env.desktop.local)
# Desktop defaults to fully offline local mode.
# Only set VITE_API_URL / VITE_SOCKET_URL if you intentionally want to connect to a remote backend.
VITE_APP_TARGET=desktop
