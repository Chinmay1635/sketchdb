# Optional local override (not committed if you rename to .env.desktop.local)
# Use this when testing against a staging API.
VITE_APP_TARGET=desktop
VITE_API_URL=https://your-staging-api.example.com/api
VITE_SOCKET_URL=https://your-staging-api.example.com
